'use client'; import {useEffect,useState} from 'react'; import Link from 'next/link'; import {api} from '../../../lib/api'; const money=(k=0)=>`₦${(Number(k)/100).toLocaleString('en-NG',{maximumFractionDigits:0})}`; export default function Page(){const[d,setD]=useState<any>();useEffect(()=>{api('/api/rider/advanced/predictions').then(setD)},[]);return <main className="rider-main advanced-page"><Link href="/advanced" className="back-link">← Advanced ROZZI</Link><span className="eyebrow">EARNINGS PREDICTION</span><h1>Plan your earnings</h1><p className="lead">A simple forecast based on your own recent rider ledger activity.</p><div className="metric-grid"><div className="metric-card card"><span>Estimated today</span><b>{money(d?.forecast?.today)}</b></div><div className="metric-card card"><span>Estimated 7 days</span><b>{money(d?.forecast?.week)}</b></div><div className="metric-card card"><span>Estimated 30 days</span><b>{money(d?.forecast?.month)}</b></div><div className="metric-card card"><span>Confidence</span><b>{d?.confidence||0}%</b></div></div><div className="card data-note">Average daily earnings: {money(d?.averageDailyEarnings)} · Average deliveries/day: {d?.averageDailyDeliveries||0}. {d?.method}</div></main>}

'use client';
import {useState} from 'react';import Link from 'next/link';import {api,post} from '../../../lib/api';
export default function Emergency(){const[sent,setSent]=useState(false),[busy,setBusy]=useState(false);async function sos(){setBusy(true);try{let delivery:any=null;try{delivery=await api('/api/rider/current-delivery')}catch{};let latitude:any,longitude:any;try{const pos=await new Promise<any>((resolve,reject)=>navigator.geolocation.getCurrentPosition(resolve,reject,{enableHighAccuracy:true,timeout:5000}));latitude=pos.coords.latitude;longitude=pos.coords.longitude}catch{};await post('/api/rider/services/sos',{deliveryId:delivery?.id,latitude,longitude,notes:'Rider activated the emergency workflow.'});await post('/api/rider/issues',{category:'ACCIDENT_EMERGENCY',subject:'Rider SOS / emergency',description:'Rider activated the emergency workflow.',deliveryId:delivery?.id});setSent(true)}catch{}finally{setBusy(false)}}return <main className="auth"><div className="authcard"><div className="brand">ROZZI RIDER</div><h1>Emergency / SOS</h1><p className="muted">If you are in immediate danger, contact local emergency services first.</p>{sent?<div className="success-banner"><b>ROZZI emergency request sent.</b><p>Support has been alerted with your active delivery context when available.</p></div>:<button className="btn danger" disabled={busy} onClick={sos}>{busy?'Sending…':'🚨 SEND SOS'}</button>}<Link className="btn ghost" href="/support">Back to Help & Safety</Link></div></main>}

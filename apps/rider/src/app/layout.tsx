import './globals.css';
export const metadata={title:'ROZZI Rider — Delivery Partner',description:'ROZZI Rider delivery command center'};
export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}

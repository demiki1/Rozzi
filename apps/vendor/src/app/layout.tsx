import './globals.css';
export const metadata={title:'ROZZI Vendor Center',description:'ROZZI merchant command center for orders, catalog, inventory, growth and finance'};
export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}

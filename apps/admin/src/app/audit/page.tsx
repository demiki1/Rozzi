'use client'; import {useEffect,useState} from 'react'; import {DashboardShell,PageHeader} from '@/components/dashboard-shell'; import {api} from '@/lib/api-client';
export default function AuditPage(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{api.get<any[]>('/api/admin/audit-logs').then(setRows).catch(()=>{})},[]);return <DashboardShell><PageHeader title="Audit Logs" description="Immutable administrative activity history."/><div className="space-y-2">{rows.map(r=><div key={r.id} className="rounded-lg border bg-white p-3 text-sm"><b>{r.action}</b> · {r.entityType} · {r.createdAt}</div>)}</div></DashboardShell>}

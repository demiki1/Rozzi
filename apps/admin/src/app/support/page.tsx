'use client'; import {useEffect,useState} from 'react'; import {DashboardShell,PageHeader} from '@/components/dashboard-shell'; import {api} from '@/lib/api-client';
export default function SupportPage(){const [rows,setRows]=useState<any[]>([]);const load=()=>api.get<any[]>('/api/admin/support/tickets').then(setRows).catch(()=>{});useEffect(() => { void load(); }, []);return <DashboardShell><PageHeader title="Support" description="Customer support tickets and operational follow-up."/><div className="space-y-3">{rows.map(r=><div key={r.id} className="rounded-xl border bg-white p-4"><div className="flex justify-between"><b>{r.subject}</b><span className="text-xs">{r.status}</span></div><p className="text-sm text-gray-500">{r.createdAt}</p></div>)}{!rows.length&&<div className="rounded-xl border bg-white p-6 text-sm text-gray-500">No tickets.</div>}</div></DashboardShell>}
